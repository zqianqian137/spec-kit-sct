# =====================================================================
# E2E 测试案例产物模板（阶段 5：speckit.testing.design —— 人工执行版）
# ---------------------------------------------------------------------
# 生成器：change-impact-e2e-bridge.py（与 *.spec.js 同批生成）
# 落盘位置：e2e/auto_generated/E2E_TESTCASES.md
# 定位：给**测试人员**执行/评审用的人类可读案例，与自动化脚本一一对应
#       （案例 ID = spec.js 文件名），人工结果可回填用于审查对账。
# =====================================================================

# E2E 测试案例（人工执行版）

**Generated**: {ISO8601 时间戳}
**SoT**: `specs/001-xxx/acceptance.yaml`
**Impact 来源**: `change-impact.md`（P0={n}, P1={n}）
**自动化脚本目录**: `e2e/auto_generated/`
**用例总数**: {n}（P0={n}, P1={n}, P2={n}）

> 使用说明：
> 1. 每个案例与同名 `{案例ID}.spec.js` 自动化脚本一一对应，可人工执行也可自动执行
> 2. 「执行结果」由测试人员回填：通过 / 失败（附缺陷单号）
> 3. 案例的 Given/When/Then 来自 SoT 验收场景原文，修改需求请先改 SoT

---

## 用例 F001-1：上传合法 CSV 创建批量任务

| 项目 | 内容 |
|------|------|
| 用例 ID | F001-1 |
| 优先级 | **P0** |
| 所属模块 | F001 批量导入测试任务 |
| 对应自动化 | `F001-1.spec.js` |

**意图**：验证已登录且有权限的用户上传合法 CSV 后，系统能创建批量任务并返回任务号

**Given（前置条件）**：
- 已登录系统且拥有导入权限
- 准备测试数据 `fixtures/tasks.csv`（2 条合法用例）

**When（操作步骤）**：
1. 登录系统
2. 打开页面 `/batch/import`
3. 在文件上传控件选择 `tasks.csv` 并提交

**Then（预期结果）**：
- 页面出现提示文本「导入成功」
- 生成批量任务号，任务状态为 PENDING

**测试数据**：`e2e/fixtures/tasks.csv`

**执行结果**：（测试人员填写）☐ 通过 ☐ 失败　缺陷单：________

---

## 用例 F001-2：重复上传同名 CSV 返回冲突

| 项目 | 内容 |
|------|------|
| 用例 ID | F001-2 |
| 优先级 | P1 |
| 所属模块 | F001 批量导入测试任务 |
| 对应自动化 | `F001-2.spec.js` |

**意图**：验证同名批量任务已存在时，再次上传返回 409 冲突且不产生新任务

**Given（前置条件）**：
- 已登录系统
- 已存在同名批量任务（可先执行 F001-1 构造）

**When（操作步骤）**：
1. 登录系统
2. 打开页面 `/batch/import`
3. 再次上传同名文件 `tasks.csv`

**Then（预期结果）**：
- 页面出现冲突提示，且任务列表数量不变

**测试数据**：`e2e/fixtures/tasks.csv`

**执行结果**：（测试人员填写）☐ 通过 ☐ 失败　缺陷单：________

---

## 执行汇总（测试人员回填）

| 用例 ID | 优先级 | 执行方式（人工/自动） | 结果 | 缺陷单 | 备注 |
|---------|--------|----------------------|------|--------|------|
| F001-1 | P0 | | | | |
| F001-2 | P1 | | | | |

> 放行标准：P0 全部通过；P1 失败需记录缺陷并限期修复。
